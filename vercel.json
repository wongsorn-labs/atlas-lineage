{
  "version": 2,
  "buildCommand": "pnpm --filter @wongsorn-labs/atlas-lineage-shared build && pnpm --filter @wongsorn-labs/atlas-lineage-db build && pnpm --filter @wongsorn-labs/atlas-lineage-api build && pnpm --filter @wongsorn-labs/atlas-lineage-web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 10,
      "includeFiles": "packages/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
